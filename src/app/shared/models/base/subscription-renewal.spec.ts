import { SubscriptionRenewal } from './subscription-renewal';

describe('SubscriptionRenewal', () => {
  it('should create an instance', () => {
    expect(new SubscriptionRenewal()).toBeTruthy();
  });
});
